internalGrpcPort: 8081
ollamaPort: 8080

modelManagerServerAddr:
modelManagerInternalServerAddr:

objectStore:
  s3:
    endpointUrl:
    bucket:

awsSecret:
  name:
  accessKeyIdKey:
  secretAccessKeyKey:

replicaCount: 1
image:
  repository: llm-operator/inference-manager-engine
  # TODO(kenji): Change this to IfNotPresent. Currently we set the pullPolicy
  # to Never as it is required to use a loaded image in a Kind cluster.
  pullPolicy: Never

ingress:
  ingressClassName:

version: latest
